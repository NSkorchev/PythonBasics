# Read user input
for h in range(24):
    for m in range(60):
        for s in range(60):
            print(f"{h:02d} : {m:02d} : {s:02d}")
        # if h < 10:
        #     hours = '0' + str(h)
        # else:
        #     hours = h
        # if m < 10:
        #     minutes = '0' + str(m)
        # else:
        #     minutes = m

            # print(f"{h} : {m} : {s}")
# Logic

# Output
